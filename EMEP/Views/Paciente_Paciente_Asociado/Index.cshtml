@model IEnumerable<EMEP.Models.Paciente_Paciente_Asociado>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Datos_Paciente.tipo_sangre)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Paciente.cedula)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Datos_Paciente.tipo_sangre)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Paciente.cedula)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.id }) |
            @Html.ActionLink("Details", "Details", new { id=item.id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.id })
        </td>
    </tr>
}

</table>
